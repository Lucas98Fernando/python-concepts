# Coerção automática ocorre quando o Python entende a operação que será feita e converte os tipos automaticamente

# Mesmo dividindo dois números inteiros, o resultado será um número real (float)
print(10 / 3)
print(type(10 / 3))

# Não existe ambiguidade, pois o True será interpretado com o valor 1
print(2 + True)
