"""
Sets (conjuntos) são utilizados para armazenar vários elementos em uma váriavel, um detalhe importante é que o Set não aceita valores repetidos, ou seja, todos os elementos duplicados serão removidos na execução.
"""

numbers = {1, 2, 3, 3, 3}

print(numbers)
print(type(numbers))

# No tamanho do set só será computado os valores únicos
print(len(numbers))
